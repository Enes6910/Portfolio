import React from 'react';

const Hero = () => (
  <section className="hero">
    <h2>Salut, moi c’est <span className="highlight">Enes</span> 👋</h2>
    <p>Développeur passionné par les interfaces modernes.</p>
  </section>
);

export default Hero;
